---
description: beactive monorepo workflow — layout, commands, the one-route-at-a-time build order, where new code goes, and commit conventions. Use when setting up the repo, running dev/build/lint, deciding which package new code belongs in, or asking what to build next.
argument-hint: [optional focus]
---

Apply the **engineering** skill for the beactive project.

Read `skills/engineering/SKILL.md` and follow it for the current task. That file is the source of
truth — this command is a thin pointer to it.

$ARGUMENTS
