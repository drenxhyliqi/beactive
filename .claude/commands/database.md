---
description: Data modeling for beactive on PostgreSQL (Supabase) with async SQLAlchemy — the engine/session setup, the four core tables, UUID/timestamp/FK conventions, per-interaction-type JSONB shapes, event-code generation, and Alembic migrations. Use when adding or changing a model, designing a JSONB payload, or writing a query.
argument-hint: [model or query]
---

Apply the **database** skill for the beactive project.

Read `skills/database/SKILL.md` and follow it for the current task. That file is the source of
truth — this command is a thin pointer to it.

$ARGUMENTS
