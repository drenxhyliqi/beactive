---
description: Two-axis review of the diff since a fixed point — Standards (does the code follow this repo's documented standards?) and Spec (does it match the originating issue/PRD?). Runs both as parallel sub-agents and reports them side by side. Use to review a branch, a PR, work-in-progress changes, or "review since X".
argument-hint: [fixed-point e.g. main or a commit SHA]
---

Apply the **review** skill for the beactive project.

Read `skills/review/SKILL.md` and execute its process. The fixed point to review against is below;
if none is given, ask for one before proceeding.

Fixed point: $ARGUMENTS
